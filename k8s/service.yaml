apiVersion: v1
kind: Service
metadata:
  name: cicd-svc
  labels:
    app: cicd
spec:
  type: LoadBalancer
  selector:
    app: cicd
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
